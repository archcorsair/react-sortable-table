{
  "name": "react-sortable-table",
  "main": "./lib/index.js",
  "version": "0.0.0",
  "description": "Sortable react table component",
  "keywords": [
    "react",
    "table",
    "sortable"
  ],
  "repository": "https://github.com/archcorsair/react-sortable-table",
  "license": "MIT",
  "scripts": {
    "test": "apm test",
    "lint": "eslint .",
    "clean": "rm -rf lib",
    "compile": "npm run clean && babel src --out-dir lib",
    "watch": "npm run clean && babel src --out-dir lib --watch"
  },
  "devDependencies": {
    "eslint-config-steelbrain": "^1.0.4",
    "jasmine-fix": "^1.0.0",
    "babel-cli": "^6.10.1",
    "babel-plugin-for-of-efficient": "^1.0.0",
    "babel-preset-steelbrain": "^5.0.0"
  },
  "dependencies": {
    "react": "^15.4.0"
  }
}
